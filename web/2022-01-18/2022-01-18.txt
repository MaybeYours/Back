useBean
내장객체에 저장된 속성값을 인스턴스로 받아서 제공하거나 , 인스턴스를 생성하여
내장객체의 속성값으로 저장하는 기능을 제공하는 태그

work 와 workgroup     

회원가입시 (id_check.jsp)
아이디 중복검사 -새로운 창을 띄워서 아이디가 사용중인지 아닌지 말해주고. 사용중이라면 다른 
	아이디 입력할수 있게 만듬. 
중복검사안해도 회원가입 되면 그냥 되기 때문에  , input hidden 태그로 아이디 중복검사 했는지 안했는지 해줘야함
중복검사를 하고 그다음에 바꿀수 없게 만들어야함\

우편번호
우편번호 db excel 을 가져와서 db에 넣어서 사용한다.
zip dao dto 만들어서 사용
String sql = "select * from zip where don like ? ||'%'order by zipcode "; 
이런식으로 앞에 '자양' 을 치면 자양번영로나 나오게 만든다.

네이버폼같은거에  찾아서 이미지와 텍스트를 같이 입력받을수 있는 api 가 있다.
