Anotation => Component-scan 을 이용하여 annotation을 처리할 수 있는 클래스가 작성된 패키지 작성
	=> 패키지 안에 있는 어노테이션 처리할 수 있게한다.
	
	component => annotation을 이용해서 bean 을 등록하는 방법 ( xml에서 bean을 등록할 수 없을때)


VO  클래스 , DTO 클래스 , JavaBean 클래스 
필드에 변수를 등록하고 , 세터게터 
	생성자는 안만들어도 되지만 , 만들게 되면 객체 생성시 초기값을 설정할 수 있다.
	
bean configuration file의 constructor-arg value 를 이용해서 값을 주입할 수 있다/
<bean clsss = "" , id =""> 	
	constructor-arg value ="1000"	
</bean>

값을 properties 파일에서 저공받아서 쓸 수 있다.
${key} 형태로 가져온다.

<bean class ="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
	<property name="locations" value="xyz/itwill05/di/student.properties"/>
</bean>
	
<bean class = "xyz.itwill05.di.Student" id ="student6">
	<property name="num" value="${num}"/>
	<property name="name" value="${name}"/>
	<property name="email" value="${email}"/>
</bean>
	

DAO 클래스를 Mybatis 로 변경하기 



Collection(set, list,map) 을 필드에 등록

Set<controller> 같이 써서 set 안에 controller의 값을 넣을 수 도 있다.

	<bean class="xyz.itwill05.di.LoginController" id="loginController"/> 를 등록하고

		<property name="controllerSet">
			<set>
				<ref bean="loginController"/>
			</set>
		</property>  이런식으로 사용
Map 도 이런식으로 보자 

Map 은 prop 으로 key 와 value로 필드 초기화 가능



REpository / service / qurifier / autowired 어노테이션 알아놓기



AOP  관점 지향 프로그래밍 


핵심 클래스 + 횡단관심모드(중복클래스)  =  합쳐서 클래스(프락시클래스(AXPECTJ))를 만든다.
ex) 회원가입 >> 회원가입기록(횡단관심) // 테이블에회원정보저장(핵심관심)

joinpoint = core 위쪽에 ? 아랫쪽에? 아니면  둘다??  				(where)
pointcut =  어떤 클래스의 어느 joinpoint 사용할꺼임? 결정해! (execution or within)	(when)
advice = 횡단관심코드 							(what)
weaving = 원래꺼에 영향 안주면서 넣게 해주는 핵심적인 과정        			  (How)

pointcut  을 execution을 이용해서 어떻게 사용할지 !

aop - before
aop - after
aop - after-returing
aop - after -throwing
aop- around

aop가 왜 필요해?ㄴ
공통된 코드를 사용하기엔 조금 힘들어서 (객체지향에서는) 









